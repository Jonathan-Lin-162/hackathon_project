To use the app, You must download the jar file and Ollama "llama3.1" model. 
First, copy this line "ollama run llama3.1" and paste it in the command prompt/terminal. 
Then type java -jar "Offline AI Assistant".jar